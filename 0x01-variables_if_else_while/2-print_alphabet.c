#include <stdio.h>

/**
 * main - A program that print alphabets
 *
 * Return: 0 (Success)
 */
int main(void)
{
    for (char letter = 'a'; letter <= 'z'; letter++) {
        putchar(letter);}
    return(0);
}
