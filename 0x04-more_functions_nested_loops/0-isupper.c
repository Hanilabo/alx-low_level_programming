#include<stdio.h>
#include"main.h"
#include<ctype.h>

/**
 * _isupper- check if uppercase
 *
 * Return: 1 if upper. 0 if not
 */
int _isupper(int c)
{
	if (_isupper(c))
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
